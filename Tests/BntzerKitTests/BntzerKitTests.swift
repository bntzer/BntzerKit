    import XCTest
    @testable import BntzerKit

    final class BntzerKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(BntzerKit().text, "Hello, World!")
        }
    }
